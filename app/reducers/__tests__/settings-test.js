/* eslint-env node, jest */
'use strict';

import {} from '../settings';

jest.autoMockOff();

describe('settings tests', () => {
  it('should write tests for settings', () => expect(true).toBeTruthy());
});
